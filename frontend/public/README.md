# Public assets

PWA icons (`/icons/icon-192.png`, `/icons/icon-512.png`, `/icons/icon-maskable-512.png`) and any static marketing imagery live here. Drop them in before production.

For Phase 1 the manifest still works with missing icon files — the browser will fall back to its default icon, which is acceptable for development.
