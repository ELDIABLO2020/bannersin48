version: "3.8"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: banners
      POSTGRES_PASSWORD: banners
      POSTGRES_DB: bannersin48
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
