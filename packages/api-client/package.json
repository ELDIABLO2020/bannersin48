{
  "name": "@bannersin48/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed API client for Banners In 48. Hand-written against the shared schemas.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mocks/browser": "./src/mocks/browser.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bannersin48/shared": "*"
  },
  "devDependencies": {
    "typescript": "~5.4.5",
    "msw": "^2.4.0",
    "vitest": "^2.0.0"
  }
}
