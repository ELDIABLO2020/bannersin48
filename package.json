{
  "name": "bannersin48",
  "version": "0.1.0",
  "private": true,
  "description": "Banners In 48 - speed-first custom banner ordering platform",
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build -w @bannersin48/design-tokens -w @bannersin48/shared -w @bannersin48/api-client -w @bannersin48/frontend",
    "build:ws": "npm run build -w @bannersin48/design-tokens -w @bannersin48/shared -w @bannersin48/api-client -w @bannersin48/frontend",
    "test": "npm run test -w @bannersin48/design-tokens -w @bannersin48/shared -w @bannersin48/api-client -w @bannersin48/frontend",
    "test:ws": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "e2e": "npm run e2e -w frontend",
    "e2e:real": "npm run e2e:real -w frontend",
    "audit:ci": "npm run audit:ci -w frontend",
    "ci": "npm run typecheck && npm run lint && npm test && NEXT_PUBLIC_ENABLE_MOCKS=1 npm run build && PORT=3010 NEXT_PUBLIC_ENABLE_MOCKS=1 CI=1 npm run e2e",
    "clean": "rimraf packages/*/dist packages/*/.turbo frontend/.next"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "~5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
